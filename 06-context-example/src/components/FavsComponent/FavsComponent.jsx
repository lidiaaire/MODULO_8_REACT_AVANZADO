import UserContext from "@/core/context/UserContext";
import React, { useContext } from "react";

export default function FavsComponent() {
  // const { token } = useContext(UserContext);
  // console.log(token);

  return (
    <div>
      <h2>FavsComponent</h2>
      {/* <span>{token}</span> */}
    </div>
  );
}
